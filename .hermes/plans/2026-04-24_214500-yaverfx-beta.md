# YaverFX Beta Sürüm Geçiş Planı

## Hedef
YaverFX'i 0.1.0 → **0.2.0-beta** sürümüne geçir. Temiz kod, aktif deploy.

---

## Mevcut Durum

### Git Status
```
Branch: master (origin/master ile sync)
Değişiklikler (staged değil):
  - app/components/Analytics.tsx      (modified)
  - app/components/RewardSystem.tsx   (modified)
  - app/components/ThemeSelector.tsx  (modified)
  - app/context/TimerContext.tsx      (modified)
  - app/globals.css                   (modified)
  - app/layout.tsx                   (modified)
  - app/page.tsx                     (modified)
  - app/utils/quickTask.ts            (DELETED)
  - public/sw.js                      (modified)

Yeni (untracked):
  - app/components/PixelHero.tsx      (yeni - SIRADA pixel hero)
  - app/components/ThemeScript.tsx     (yeni - theme/init script)

Deployed: Vercel (proje: yaver-fx, org: team_ixYsGpgFJMbE5bwlYR3kFfVM)
```

---

## Adım 1: Kod Temizliği & Dead Code Kontrolü

- [ ] `quickTask.ts` dosyası silinmiş → import/reference yok (doğrulanacak)
  ```bash
  grep -r "quickTask" --include="*.ts" --include="*.tsx" . | grep -v node_modules
  ```
- [ ] `Analytics.tsx`'te `STORAGE_KEY` → `STATS_KEY` rename edildi (daily log eklendi)
- [ ] Yeni dosyalar (`PixelHero.tsx`, `ThemeScript.tsx`) kontrol edilecek
- [ ] `TimerContext.tsx`'te 17 satır eksiltme - gereksiz kod mu?
- [ ] `layout.tsx`'te 17 satır eksiltme - ThemeScript artık import ediliyor
- [ ] `page.tsx`'te 120 satır değişiklik - temizlik mi yoksa yeni özellik mi?

---

## Adım 2: Versiyon Bump

`package.json`:
```json
"version": "0.2.0-beta"
```

---

## Adım 3: Git Add + Commit

```bash
git add -A
git commit -m "feat: beta release 0.2.0 — pixel hero, daily logs, SIRADA theme, dead code cleanup"
```

---

## Adım 4: Push & Vercel Deploy

```bash
git push origin master
# Vercel otomatik tetiklenecek (zaten linked)
```

---

## Adım 5: Deploy Doğrulama

- [ ] `https://yaver-fx.vercel.app` açılıyor mu?
- [ ] PWA manifest doğru mu?
- [ ] Service worker register oluyor mu?
- [ ] Konsolda error var mı? (browser console check)

---

## Değişecek Dosyalar

| Dosya | Değişiklik |
|-------|-----------|
| `package.json` | versiyon → 0.2.0-beta |
| `app/components/PixelHero.tsx` | **YENİ** — SIRADA pixel hero |
| `app/components/ThemeScript.tsx` | **YENİ** — theme/init script |
| `app/components/Analytics.tsx` | daily log, yeni stats key |
| `app/components/RewardSystem.tsx` | küçük değişiklik |
| `app/components/ThemeSelector.tsx` | 1 satır değişiklik |
| `app/context/TimerContext.tsx` | 17 satır eksiltme |
| `app/globals.css` | 144 satır değişiklik |
| `app/layout.tsx` | 17 satır eksiltme, ThemeScript import |
| `app/page.tsx` | 120 satır değişiklik |
| `app/utils/quickTask.ts` | **SİLİNDİ** (dead code) |
| `public/sw.js` | 2 satır değişiklik |

---

## Riskler & Notlar

1. **TimerContext eksiltme**: 17 satır eksiltme neyi kaldırdı? Gerekli bir şey mi?
2. **PixelHero**: React Three Fiber bağımlılığı var mı? Build'de sorun çıkarabilir
3. **quickTask.ts**: Hiçbir yerde import edilmiyor → temiz silme
4. **Vercel deployment**: Zaten `.vercel/project.json` var → otomatik deploy
